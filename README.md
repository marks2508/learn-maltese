## Installation instructions

#### Prerequisites
Mongo DB - installed && started.

#### Setup

`$ npm install`

`$ yarn install` ??

`$ npm run start:server`

`$ npm run seed`

`$ npm run start:client`